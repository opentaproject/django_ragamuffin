export VERSION=latest
cd "$(dirname "$0")"
echo creating docker ragamuffin-nginx:${VERSION}
#eval $(minikube docker-env)
docker buildx build --platform linux/amd64  -t opentaproject/ragamuffin-nginx:${VERSION} --push .
#docker push opentaproject/ragamuffin-nginx:${VERSION}
